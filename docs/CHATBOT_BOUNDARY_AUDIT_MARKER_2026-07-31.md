# Audit marker

Known direct chatbot write to `public.quote_requests`: removed on the protected branch.

Static regression gate: present and wired into build.

Final merge status: pending exact-head PR validation.
