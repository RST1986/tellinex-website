# Status

Ready for protected pull-request validation.

The branch is expected to be ahead of `main` only, with no dependency on database or secret changes. Merge is permitted only after the branch preview/build gates succeed on the exact head.
